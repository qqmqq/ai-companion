{
  "name": "ai-companion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "dev": "node scripts/dev.ts",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "guard": "pnpm --filter @companion/backend guard"
  },
  "packageManager": "pnpm@10.0.0"
}
